#include "../headers/element.h"

/*

int initialize_elements(t_element *elements)
{
    
    elements = (t_element *)malloc(sizeof(t_element));
    if (elements != NULL)
    {
        map->total_elements = 0;
        map->max_col = 0;
        map->max_fil = 0;
        set_elements(map->elements);
        if (!map->elements)
        {
            free(map);
            perror(ERROR_MEMORY);
            return (ERROR);
        }
        return (SUCCESSFUL);
    }
    perror(ERROR_MEMORY);

    return (ERROR);
    
   return (SUCCESSFUL);
}
*/